#include <bits/stdc++.h>
using namespace std;

int main() {
  vector<int> vec(3);
  cin >> vec.at(0) >> vec.at(1) >> vec.at(2);
  // 和を出力
  cout << vec.at(0) + vec.at(1) + vec.at(2) << endl;
}

// for文で回す
int main() {
  vector(int) vec(100);
  for (int i = 0; i < 100; i++) {}
    cin >> vec.at(i);
  }
}
